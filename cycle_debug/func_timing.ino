/*
void func_timing(){
  long iTime0_usec;
  float executionTime_usec;
  
  pinMode(2, OUTPUT);

  iTime0_usec = micros();
  for(int i = 1; i <= 1000; i++){
    V_ADC1_adu = analogRead(1);
  }
  executionTime_usec = (micros() - iTime0-usec) / 10000.0;
  SerialUSB.println(executionTime_usec, 3);
}
*/
